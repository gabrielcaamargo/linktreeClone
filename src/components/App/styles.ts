import styled from "styled-components";

export const ThemeSwitcher = styled.div`
  width: 100%;
  display: flex;
  justify-content: flex-end;

  button {
    background: none;
    outline: none;
    border: none;
    padding: 16px;
    font-size: 24px;
  }

`;
